# No environment configuration is required to run TerraComrad.
#
# The app currently does NOT call any external LLM/API. The viewer (doctor
# annotation) and the deterministic system analysis run fully locally.
#
# LLM provider settings will be reintroduced here when the AI-assisted,
# annotation-driven explanation layer is rebuilt.
